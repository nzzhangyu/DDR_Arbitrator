# CT 数据采集板卡 FPGA 逻辑设计 - 面试准备稿

## 1. 简历项目经历

### 项目描述

面向 CT 探测器数据采集系统，负责 DASI-BGX02N ASIC 接口 FPGA 逻辑开发，完成多通道探测器数据采集、ASIC 控制配置、图像数据预处理及高速上传链路设计。

### 核心工作

- 负责 28 路 BGX ASIC LVDS 数据接收、过采样、串并转换、帧同步检测及通道数据对齐，完成 26 bit 原始串行数据到 FPGA 内部并行数据的接收链路设计。
- 搭建覆盖控制、采集、处理和输出环节的多时钟域图像数据链路，基于 40 MHz 控制域、100 MHz 数据缓存/输出域、150 MHz 图像处理域及 200 MHz 过采样域，结合 CDC 同步、FIFO/BRAM 缓存、乒乓 Buffer 和 ROM 地址映射，实现权重校准、暗场扣除、对数 LUT 压缩、26 bit 到 16 bit 数据压缩及切片数据重排。
- 设计 ASIC 控制与数据输出逻辑，通过状态机完成上电初始化、校准模式切换、寄存器读写和回读校验，并结合错误锁存、状态监测及通信回读机制，实现处理后图像数据通过 4 路 FTP DDR/LVDS 通道连续输出。

### 技术栈

LVDS、多通道过采样、串并转换、DDR 输出、SPI、CDC、FIFO、BRAM、乒乓 Buffer、LUT 压缩、状态机设计。

## 2. 1 分钟项目介绍

我做的这个项目是 CT 探测器数据采集板卡上的 FPGA 逻辑，主要负责连接 BGX02N ASIC 和上级 DASI/Rcomm 系统。

我的工作可以分成三部分。第一部分是采集链路，FPGA 从 28 路 BGX ASIC LVDS 接口接收原始探测器数据，通过多通道过采样、串并转换、帧同步检测和通道对齐，把 ASIC 输出的 26 bit 串行数据转换成 FPGA 内部并行数据。

第二部分是图像数据处理链路。工程中包含 40 MHz、100 MHz、150 MHz、200 MHz 多个时钟域，分别承担控制通信、数据缓存与输出调度、图像预处理和 LVDS 过采样。我在链路中使用 CDC 同步、FIFO/BRAM 缓存、乒乓 Buffer 和 ROM 地址映射，完成权重校准、暗场扣除、对数 LUT 压缩以及切片数据重排。

第三部分是控制和输出。通过状态机完成 ASIC 上电初始化、校准模式切换、SPI 寄存器读写和回读校验，同时把处理后的图像数据通过 4 路 FTP DDR/LVDS 通道连续输出给上级系统。

## 3. 2-3 分钟详细讲稿

这个项目的背景是 CT 探测器前端会输出多通道、高速、连续的数据流，FPGA 需要在板卡上完成数据接收、预处理、缓存、重排和上传，同时还要负责 ASIC 的配置和状态监测。

我主要负责 DASI-BGX02N ASIC 接口相关的 FPGA 逻辑设计。整体链路可以分为采集、处理、控制和输出四个部分。

在采集部分，BGX ASIC 通过多路 LVDS 输出探测器数据。FPGA 侧使用 200 MHz 时钟对输入 LVDS 数据做过采样，再完成串并转换，把每个通道的串行数据恢复成 26 bit 并行数据。恢复后还要检测帧头、帧尾和温度字段，并把各通道数据对齐，保证后级处理时同一拍对应同一帧的有效采样点。

在多时钟域方面，工程里 40 MHz 主要用于系统控制、Rcomm 命令解析和状态回读；100 MHz 主要用于数据接收缓存和后级输出调度；150 MHz 用于图像预处理；200 MHz 用于 LVDS 输入过采样。不同环节之间通过 CDC 同步、FIFO 和 BRAM 做解耦，避免控制域、采集域、处理域和输出域之间出现亚稳态或者吞吐不匹配。

图像预处理部分主要包括权重校准、暗场扣除、对数 LUT 压缩和切片重排。权重校准用于修正不同通道和像素点之间的增益差异；暗场扣除用于去掉本底偏置；对数 LUT 压缩则是从 CT 成像原理出发，把探测器采到的线性强度数据转换成对数域衰减数据，并压缩成 16 bit 输出；切片重排使用 ROM 地址映射配合乒乓 Buffer，把 ASIC 的采集顺序重新映射成上级系统需要的图像输出顺序。

控制部分主要是 ASIC SPI 配置状态机。上电后 FPGA 先按时序拉起相关电源和复位，再通过 SPI 完成 ASIC 初始化配置。运行过程中也支持校准模式切换、寄存器读写、回读校验和错误状态锁存。最后，处理后的多通道图像数据会通过 4 路 FTP DDR/LVDS 通道连续输出。

这个项目中我比较有收获的地方是对高速采集链路的整体理解更完整了：不仅要把数据接进来，还要处理好多时钟域、缓存结构、帧同步、位宽压缩、输出带宽和系统可观测性这些问题。

## 4. 重点技术解释

### 4.1 多时钟域设计

这个工程不是单一时钟逻辑，而是按功能拆成多个时钟域：

- 40 MHz：控制域，用于 Rcomm 命令解析、状态回读、SPI 控制和 LED/状态监测。
- 100 MHz：数据缓存与输出调度域，用于接收缓存、读 RAM、组织 FTP 输出节奏。
- 150 MHz：图像处理域，用于权重校准、暗场扣除、对数 LUT 压缩和切片数据整理。
- 200 MHz：输入采样域，用于 LVDS 输入数据的过采样和边沿恢复。

设计重点是不能直接跨时钟使用信号。单 bit 控制信号一般用同步寄存器或边沿同步，多 bit 数据流通过 FIFO、BRAM 或握手机制跨域，帧同步信号也需要在目标时钟域重新打一拍并检测边沿。

### 4.2 对数 LUT 压缩

CT 探测器采集的是 X 射线穿过人体后的剩余强度，物理模型可以表示为：

```text
I = I0 * e^(-∫μdl)
```

后端重建需要的是衰减积分：

```text
∫μdl = ln(I0 / I)
```

所以 FPGA 中做对数 LUT 压缩，不只是为了减少位宽，也是为了把线性强度数据转换成更符合 CT 重建需求的对数域数据。

硬件实现上，直接实时计算 log 资源开销较大，所以采用 LUT 近似。基本思路是先找输入数据的最高有效位，得到对数结果的整数部分；再用输入数据的尾数部分生成 LUT 地址，查表得到对数小数部分；最后把整数部分和小数部分拼成 16 bit 定点结果。

可以简单概括为：

```text
26 bit 线性强度数据 -> 最高位检测 -> LUT 查表近似 log 小数 -> 舍入截位 -> 16 bit 对数域图像数据
```

### 4.3 ROM 地址映射与乒乓 Buffer

切片数据重排不是靠大规模组合交换完成，而是把重排规则预先存在 ROM 中。输入数据每来一个有效点，模块根据当前 ASIC、FTP 和采样计数查 ROM，得到目标写地址，再把数据写入 BRAM 中对应位置。

乒乓 Buffer 用于解决读写并行问题。一帧数据写入 Bank0 时，输出端可以从 Bank1 读取上一帧；下一帧再交换读写 Bank。这样可以避免同一块 RAM 同时读写冲突，也能让 150 MHz 处理域和 100 MHz 输出域相互解耦。

### 4.4 log_cal 代码片段说明

以最高位为 `data_in[28]` 的分支为例：

```verilog
if(data_in[28]) begin
    int_buf    <= 5'd24;
    lut24_addr <= {data_in[27:15]};
    lut11_addr <= {data_in[27:18], (data_in[14]^data_in[13]), (data_in[14]^data_in[12])};
    lut08_addr <= {data_in[27:18], (data_in[11]^data_in[10]), (data_in[11]^data_in[9])};
    lut05_addr <= {data_in[27:18], (data_in[8]^data_in[7]), (data_in[8]^data_in[6]), (data_in[8]^data_in[5])};
    msb_buf    <= {data_in[14], data_in[11], data_in[8]};
end
```

这段代码是在输入数据最高有效位为 bit28 时，生成对数运算需要的整数部分和小数查表地址。`int_buf` 表示当前数据量级对应的对数整数部分；`lut24_addr` 使用最高位之后的 13 位尾数查主 LUT；`lut11_addr`、`lut08_addr`、`lut05_addr` 使用更低位生成补偿 LUT 地址；`msb_buf` 保存几个关键位，用于后续决定补偿项加减。整体作用是把高位宽输入数据归一化后，拆成“整数部分 + 小数 LUT 近似部分”，为最终 16 bit 定点对数输出做准备。

### 4.5 状态机控制 ASIC

ASIC 控制逻辑采用状态机组织，主要包括：

- 上电初始化配置；
- 校准模式配置；
- 正常模式恢复；
- SPI 寄存器写；
- SPI 寄存器读；
- 回读校验和错误锁存。

这样做的好处是配置流程清晰，便于控制时序，也方便后续通过 Rcomm 命令触发模式切换和寄存器访问。

## 5. 面试常见追问回答

### 问：为什么要做过采样？

BGX ASIC 输出的是高速 LVDS 串行数据，直接在单一采样点采样容易受相位偏移和边沿抖动影响。使用更高频时钟做过采样，可以提高数据恢复的鲁棒性，便于后续判断有效采样点并完成串并转换。

### 问：为什么要用 FIFO/BRAM？

主要是两个原因。第一是跨时钟域解耦，不同模块工作在不同频率下，不能直接传递多 bit 数据。第二是吞吐匹配，采集、处理和输出节奏不完全一致，需要缓存吸收短时间的数据速率差异。

### 问：对数 LUT 压缩是不是简单截位？

不是。简单截位只是丢掉低位精度，而这个模块是先根据 CT 衰减模型做对数域变换，再输出 16 bit 定点结果。它既完成动态范围压缩，也把探测器线性强度数据转换成后级重建更需要的衰减相关数据。

### 问：为什么切片重排要用 ROM？

因为通道采集顺序和图像输出顺序之间存在固定映射关系。如果用组合逻辑写死，代码复杂且不易维护；把映射关系放到 ROM 中，逻辑只负责查地址和写 RAM，结构更清晰，也方便后续替换不同的排序表。

### 问：项目中你觉得最难的点是什么？

我认为难点主要在数据链路的连续性和跨时钟域稳定性。采集端、处理端和输出端频率不同，而且数据是连续帧结构，任何一个环节的帧同步、缓存切换或读写时序出错，都会导致图像错位。因此设计时需要明确每个时钟域的职责，用同步、FIFO、BRAM 和乒乓结构把各段链路解耦。

## 6. 简短收尾

这个项目让我对 FPGA 在医疗影像前端数据链路中的作用有了比较完整的理解：FPGA 不只是做接口转换，还要承担高速采集、多时钟域缓存、定点图像预处理、ASIC 控制和系统状态监测。通过这个项目，我对 LVDS 接收、CDC、BRAM/FIFO 缓存、状态机和 LUT 算法硬件化都有了更深入的实践经验。
