# Rcomm 高速数据链路项目点实现说明

本文用于解释简历中“旋转通信板 Rcomm 高速数据链路固件开发”项目点背后的实现方式。定位是面试讲稿型材料：比简历更详细，但不展开到逐行代码。

## 1. 项目总览

### 1.1 项目定位

该项目面向 CT 旋转通信链路中的连续高速数据传输需求，核心目标是把跨板接收到的连续数据稳定缓存到 DDR4，再按 view 粒度读出，送入 GTY 8B/10B 发送链路。

整体链路可以概括为：

```text
跨板接收数据
  -> 写侧 FIFO
  -> DDR4 环形缓存
  -> 读侧 FIFO / view 级读缓存
  -> 发送侧跨时钟 FIFO
  -> GTY 8B/10B 双 lane 发送链路
```

其中 DDR4 缓存调度负责解决“连续数据如何不丢、不读空、可回退”的问题；GTY 8B/10B 发送链路负责解决“DDR 读出的 view 数据如何稳定送入高速串行发送路径”的问题。

### 1.2 关键指标

- DDR 用户侧数据通路为 `128-bit`。
- DDR 用户口理论带宽约 `4.8 GB/s`。
- GTY 发送链路采用双 lane `10.3125 Gbps` line rate。
- 双 lane 扣除 8B/10B 编码后的理论数据吞吐约 `16.5 Gbps`。

这些指标用于说明链路量级：DDR 缓存侧的理论供数能力高于 GTY 8B/10B 发送侧的理论数据吞吐，系统长期连续发送时更需要关注缓存调度、读侧预取、发送链路反压和业务回退机制。

### 1.3 项目边界

- DDR4 部分重点是环形缓存、动态水位仲裁、view 级读出和 replay/backtracking。
- 发送部分重点是 GTY 8B/10B 发送链路重构、发送侧数据调度和诊断能力。
- Aurora 64B/66B IP 在项目中用于跨板接收，但主要是 IP 调用和接口适配，不作为简历核心亮点展开。
- 本文不描述滑环物理验证场景，只说明 RTL 架构、缓存调度和链路发送逻辑。

---

## 2. 简历项目点版本

### 旋转通信板 Rcomm 高速数据链路固件开发

**项目描述：**  
面向 CT 旋转通信链路中的连续高速数据传输需求，设计 DDR4 环形缓存调度与 GTY 8B/10B 发送链路，实现跨板接收数据写入 DDR、按 view 粒度读出、缓存调度、链路发送和通信异常下的 view 级回退重传。DDR 用户侧数据通路为 `128-bit`，理论用户口带宽约 `4.8 GB/s`；GTY 发送链路采用双 lane `10.3125 Gbps` line rate，扣除 8B/10B 编码后的理论数据吞吐约 `16.5 Gbps`。

**项目职责：**  
负责 Rcomm 高速数据链路中 DDR 环形缓存调度、动态水位仲裁、view 级读出控制、回退重传机制、GTY 8B/10B 发送链路重构、CRC 诊断和链路状态监控等 RTL 设计与验证工作。

**核心工作：**

- 设计 DDR4 环形缓存调度架构，基于写侧 FIFO、读侧 FIFO 和发送侧跨时钟 FIFO 组织数据流，实现跨板接收数据连续写入、按 view 粒度读出、预取缓存和发送供数。
- 实现动态水位仲裁与环形地址管理，根据写侧高水位、读侧低水位和读侧高水位切换写入排空、读侧预取和停止预取策略，并维护写地址、读地址和回退地址，降低连续传输中的 FIFO 溢出和读空风险。
- 设计 view 级管理和 replay/backtracking 机制，根据 view 写完成、读出进度、发送进度和通信确认状态，控制 DDR 读指针回退到指定 view 起点并重新触发发送流程。
- 搭建 DDR fast mock 模型，针对连续写入、读侧反压和读返回延迟等极端情况验证缓存调度策略，重点评估 FIFO 溢出、读空和回退读流程稳定性。
- 重构 GTY 8B/10B 发送链路，采用弹性 FIFO 与发送状态机替代原硬延迟线结构，将数据缓存、链路调度和控制阶段解耦，优化 payload 连续输出和链路空拍处理。
- 实现发送侧 CRC、frame tag 检查、first-view 标记和 no-EOF watchdog 诊断逻辑，便于通过状态机、FIFO 水位和发送计数定位链路空拍、帧边界和数据异常问题。

---

## 3. 项目点实现说明

## 3.1 DDR4 环形缓存调度架构

**简历表述：**  
设计 DDR4 环形缓存调度架构，基于写侧 FIFO、读侧 FIFO 和发送侧跨时钟 FIFO 组织数据流，实现跨板接收数据连续写入、按 view 粒度读出、预取缓存和发送供数。

**实现方式：**

DDR 缓存调度不是把上游数据直接写入 DDR 再简单读出，而是通过多级缓冲把连续流数据拆成几个相对独立的阶段。

写入侧先用写侧 FIFO 吸收跨板接收链路或上游模块送来的连续数据。DDR 控制逻辑在 `ui_clk` 域读取写侧 FIFO，并以 burst 事务写入 DDR。这样做可以把上游连续输入和 DDR 写事务之间的瞬时 ready/valid 波动隔离开。

DDR 中的数据按环形缓存方式组织。写地址持续递增，到达缓存空间边界后回绕，因此系统可以长期循环写入连续数据，而不是每个 view 单独分配独立地址空间。读侧根据 view 状态和发送需求，从 DDR 中预取数据到读侧 FIFO。发送侧再通过跨时钟 FIFO，把 `ui_clk` 域的数据送入 GTX/GTY 用户时钟域。

这几个缓冲层的作用不同：

- 写侧 FIFO：吸收上游突发输入，保护 DDR 写侧。
- DDR 环形缓存：提供大容量缓存和历史 view 保留空间。
- 读侧 FIFO：把 DDR 读返回数据提前缓存，避免发送端直接等待 DDR。
- 发送侧跨时钟 FIFO：隔离 DDR `ui_clk` 和 GTY 用户时钟域。

**关键机制/信号：**

- 写侧 FIFO 水位：用于判断是否需要优先写 DDR。
- 读侧 FIFO 水位：用于判断是否需要预取或停止预取。
- `ddr_rd_req`：DDR 读请求，由 view 读控制和缓存状态共同决定。
- `user_r_valid`：DDR 读返回数据有效，用于统计当前 view 的读出进度。
- `aurora_asy_fifo_almost_full`：发送侧跨时钟 FIFO 接近满时抑制继续读 DDR。

**解决的问题：**

该结构降低了三个风险。第一，上游连续输入不会因为 DDR 短期反压而直接丢数；第二，发送侧不会因为 DDR 读返回延迟而频繁读空；第三，DDR 读写和 GTY 发送处于不同节奏时，可以通过 FIFO 解耦。

**面试展开说法：**

可以这样讲：

> 这个模块的核心不是单次 DDR 读写，而是连续流缓存。写侧 FIFO 负责接住跨板输入，DDR 环形缓存负责提供大容量回放空间，读侧 FIFO 和发送侧跨时钟 FIFO负责给高速发送链路稳定供数。这样可以把 DDR 访问的不确定性和 GTY 发送的连续性分开处理。

---

## 3.2 动态水位仲裁与环形地址管理

**简历表述：**  
实现动态水位仲裁与环形地址管理，根据写侧高水位、读侧低水位和读侧高水位切换写入排空、读侧预取和停止预取策略，并维护写地址、读地址和回退地址，降低连续传输中的 FIFO 溢出和读空风险。

**实现方式：**

DDR 访问由读写仲裁模块统一调度。仲裁逻辑持续观察写侧 FIFO 和读侧 FIFO 的水位，并根据当前缓存压力选择优先写还是优先读。

当写侧 FIFO 达到高水位时，系统优先执行 DDR 写事务，尽快把上游堆积的数据写入 DDR，降低写 FIFO overflow 风险。当读侧 FIFO 低于低水位时，系统优先执行 DDR 读事务，提前为发送链路补充数据，降低发送侧读空风险。当读侧 FIFO 达到高水位时，系统停止继续预取，避免读侧缓存过满影响后续调度。

读写事务以 burst 为单位执行，减少读写方向频繁切换造成的开销。写侧还带有 tail aging 逻辑，避免尾部不足完整 burst 的数据长期滞留在写 FIFO 中。读侧带有等待老化逻辑，在读需求长期得不到服务时提高读侧服务机会。

环形地址管理维护三类地址：

- 写地址：随 DDR 写入推进，到边界后回绕。
- 读地址：随 view 读出推进。
- 回退地址：用于通信异常时重新读出最近 view。

**关键机制/信号：**

- `WR_LEVEL_URGENT`：写侧紧急水位，用于强制优先写。
- `RD_LEVEL_URGENT`：读侧低水位，用于触发读侧预取。
- `RD_LEVEL_HIGH`：读侧高水位，用于停止继续预取。
- `wr_level_urgent`：写侧是否处于紧急状态。
- `rd_level_urgent`：读侧是否需要紧急补数。
- `rd_fifo_can_prefetch`：读侧 FIFO 是否允许继续预取。
- `write_burst_len` / `read_burst_len`：当前服务窗口长度。

**解决的问题：**

固定写优先策略虽然能保护写侧，但可能导致读侧长时间得不到数据，发送端出现读空。固定读优先又可能让上游写数据堆积。动态水位仲裁的目标是在连续流场景下根据缓存压力动态切换优先级，让写侧 overflow 和读侧 underflow 都尽量不发生。

**面试展开说法：**

可以这样讲：

> 我没有采用单一固定优先级，而是把写侧和读侧都抽象成缓存压力。写侧高水位说明上游快溢出了，读侧低水位说明发送端快断流了，读侧高水位说明不能再预取。仲裁逻辑根据这些状态选择读写服务窗口，本质是在保护写不丢和读不断之间做动态平衡。

---

## 3.3 View 级管理

**简历表述：**  
设计 view 级读缓存控制逻辑，根据 view 写完成、读出进度和发送状态维护 DDR 读地址、发送进度和缓存状态，实现按 view 粒度组织数据读出。

**实现方式：**

系统把 view 作为业务数据组织单位。`rd_cache_ctrl` 里同时维护写侧 view 计数、读侧 view 计数、当前 view 内部 beat 计数和最近读地址。

上游每写完一个 view，会产生 `view_Reading_Done`。这个信号通过跨时钟脉冲同步到 DDR `ui_clk` 域，形成 `wr_view_pulse`。每来一个 `wr_view_pulse`，`wr_view_num` 自增，表示 DDR 中已经写好的 view 数量增加。

读侧通过 `user_r_valid` 统计当前 view 已经读出的 128-bit beat 数。`rd_data_cnt` 从 0 开始累加，当它达到 `view_size - 1` 且当前 beat 有效时，产生 `sample_frame_rd_done`。这个信号表示当前 view 的 DDR 读出已经完成。

读完一个 view 后，`rd_view_num` 自增，表示已经读出并送往发送链路的 view 数量增加。读地址方面，`rd_latest_addr` 以 view 为粒度维护最近读出的 view 地址，每完成一个 view，就加上 `view_size`，指向下一个 view 起点。

DDR 读请求并不是随时发起，而是在满足以下条件时才允许：

- 发送侧 FIFO 没有接近满。
- 当前不是 idle 发送过程。
- 当前不是 refresh 过程。
- TX channel up。
- `wr_view_num > rd_view_num`，即已经写好的 view 数量大于已经读出的 view 数量。

因此，该模块实现的是 view 粒度的读控制：只有完整 view 可读时才启动读请求，读满 `view_size` 后认为一个 view 完成。

**关键机制/信号：**

- `view_Reading_Done`：上游 view 写完成标记。
- `wr_view_pulse`：同步到 `ui_clk` 域后的写 view 完成脉冲。
- `wr_view_num`：已写入 DDR 的 view 计数。
- `rd_view_num`：已从 DDR 读出的 view 计数。
- `view_size`：一个 view 包含的 128-bit beat 数。
- `rd_data_cnt`：当前 view 内已读出的 beat 数。
- `sample_frame_rd_done`：当前 view 读出完成脉冲。
- `rd_latest_addr`：最近 view 读地址，用于推进和回退。
- `wr_gt_rd`：`wr_view_num > rd_view_num`，表示有 view 可读。

**解决的问题：**

如果只按连续地址读 DDR，发送侧很难知道业务边界在哪里，也很难在通信异常时回到正确位置。view 级管理把连续 DDR 数据流提升成业务粒度，使读出、发送完成、最后 view 判断和回退重传都有明确边界。

**面试展开说法：**

可以这样讲：

> view 管理的核心是两个计数器和一个 view 内计数器。`wr_view_num` 表示已经写好的 view，`rd_view_num` 表示已经读走的 view，`rd_data_cnt` 配合 `view_size` 判断当前 view 是否读完。只有 `wr_view_num > rd_view_num` 时才允许读 DDR，读满一个 view 后读计数推进，地址也按 `view_size` 跳到下一个 view 起点。

---

## 3.4 View Replay / Backtracking

**简历表述：**  
设计 view replay/backtracking 机制，根据 view 写完成、发送进度和通信确认状态，控制 DDR 读指针回退到指定 view 起点并重新触发发送流程。

**实现方式：**

view replay 由 `rd_cache_ctrl` 和 `comm_ok` 协同实现。`rd_cache_ctrl` 负责维护最近 view 的读地址和可回退深度；`comm_ok` 负责根据通信确认状态判断是否需要触发回退。

`rd_cache_ctrl` 中的 `rp_back_num` 用于记录最近可回退的 view 数，当前逻辑最多保留最近两个 view 的回退能力。每读完一个 view，如果还没达到上限，`rp_back_num` 增加。触发回退后，`rp_back_num` 清零，重新开始统计。

回退地址通过 `rd_latest_addr` 和 `view_size` 计算。如果最近只需要回退一个 view，则回退偏移为 `view_size`；如果需要回退两个 view，则偏移为 `view_size << 1`。触发 `rp_back_en_i` 时，模块计算：

```text
rp_back_view_addr = rd_latest_addr - back_offset
```

同时，`rd_latest_addr` 也回退到相同位置，`rd_view_num` 减去对应的 `rp_back_num`，保证 view 计数和地址同步回退。

`comm_ok` 模块负责判断通信确认。每完成一个 view 后，系统进入等待通信确认状态。如果在超时前收到确认，则产生 `view_trans_ok`，认为当前 view 传输成功。如果等待期间又出现新的 view 完成事件，或者等待超时，则认为前序 view 未被正常确认，触发 `rp_back_en_t`。

`rp_back_en_t` 后续生成多个控制信号：

- `rp_back_en_i`：给 `rd_cache_ctrl`，用于计算回退 view 地址和更新 view 计数。
- `rp_back_en`：给 DDR 命令生成模块，用于真正回退 DDR 读指针。
- `rp_back_en_rst`：用于复位/重启发送侧相关状态，避免旧发送状态残留。
- `rp_back_cnt_add_en`：给系统侧统计或状态逻辑，记录回退事件。

DDR 命令生成模块收到 `rp_back_en` 后，将内部 DDR 读指针改为 `rp_back_view_addr`，后续重新从该 view 起点读出数据。

**关键机制/信号：**

- `rp_back_num`：最近可回退的 view 数。
- `back_offset`：根据 `view_size` 和 `rp_back_num` 计算的回退偏移。
- `rp_back_view_addr`：最终输出给 DDR 读指针的回退地址。
- `rp_back_en_i`：回退地址计算和 view 计数回退触发。
- `rp_back_en`：DDR 命令层读指针回退触发。
- `rp_back_en_rst`：发送侧状态复位/重启窗口。
- `view_trans_ok`：通信确认成功标记。
- `comm_ok_timeout`：通信确认超时条件。

**解决的问题：**

高速链路出现通信确认异常时，如果只继续向后发送，会导致接收端缺 view 或数据错位。view replay 让系统可以回到最近 view 起点重新发送，重传边界清晰，不会从 view 中间恢复，降低业务数据错位风险。

**面试展开说法：**

可以这样讲：

> 回退不是按任意地址做的，而是按 view 边界做。读控制模块维护最近 view 起点，通信确认模块判断是否需要重传。一旦需要回退，先算出 `rp_back_view_addr`，再让 DDR 读指针回到这个 view 起点，同时复位发送侧状态。这样重传从业务边界开始，接收侧更容易重新对齐。

---

## 3.5 DDR Fast Mock 验证

**简历表述：**  
搭建 DDR fast mock 模型，针对连续写入、读侧反压和读返回延迟等极端情况验证缓存调度策略，重点评估 FIFO 溢出、读空和回退读流程稳定性。

**实现方式：**

完整 MIG 仿真模型运行成本较高，不适合频繁覆盖调度边界。因此项目中引入 fast mock，用轻量 DDR 行为模型替代完整 MIG，重点验证缓存调度是否稳定，而不是精确复现 DDR 物理时序。

fast mock 主要模拟会影响调度稳定性的行为：

- 连续写入压力：上游数据持续进入写 FIFO，观察动态水位仲裁能否及时给写侧服务。
- 读侧反压：发送侧或读 FIFO 暂时不能继续接收，观察预取是否能被正确停止。
- 读返回延迟：DDR 读命令和数据返回之间加入变化延迟，观察发送侧是否容易读空。
- 回退读流程：触发 replay/backtracking 后，观察读地址是否回退到正确 view 起点，状态机是否能重新稳定读出。

验证时重点观察 FIFO 水位、读写 grant、读地址、写地址、回退地址、overflow/underflow 标记和状态机是否卡死。

**关键机制/信号：**

- 写侧 FIFO 水位和 overflow 标记。
- 读侧 FIFO 水位和 underflow 风险。
- `ddr_rd_req` / 写请求 grant。
- `user_r_valid` / 读返回有效。
- `rp_back_en` / `rp_back_view_addr`。
- 读写状态机当前状态。

**解决的问题：**

fast mock 的价值是快速把缓存调度推到边界情况，提前发现“连续写入时写 FIFO 是否会溢出”“读返回变慢时发送侧是否会读空”“回退后读地址是否错位”等问题。它不是替代真实 MIG 验证，而是用于快速收敛调度策略。

**面试展开说法：**

可以这样讲：

> 我没有把 fast mock 当成 DDR 精确模型，而是把它当成调度压力发生器。它重点制造连续写入、读侧反压、读返回延迟和回退读这些边界条件，用来观察 FIFO 水位和读写状态机是否稳定。这样比每次跑完整 MIG 仿真更快，也更适合迭代仲裁策略。

---

## 3.6 GTY 8B/10B 发送链路重构

**简历表述：**  
重构 GTY 8B/10B 发送链路，采用弹性 FIFO 与发送状态机替代原硬延迟线结构，将数据缓存、链路调度和控制阶段解耦，优化 payload 连续输出和链路空拍处理。

**实现方式：**

原发送链路依赖硬延迟线对齐数据和控制时序。硬延迟线结构的问题是：数据位置、控制符号、帧边界和 CRC 时序耦合在一起，调试时很难判断当前空拍是上游没有数据、延迟线对齐问题，还是控制阶段占用。

重构后，发送入口先通过弹性 FIFO 接收上游数据。FIFO 负责缓存和流量平滑，发送状态机负责决定当前周期输出 payload、空闲数据还是控制阶段数据。这样数据缓存和链路调度被拆开，状态机可以根据 FIFO empty/full、当前发送状态和链路状态做明确决策。

这种结构的好处是：

- payload 数据是否连续，主要看 FIFO 供数和状态机调度。
- 控制阶段是否占用链路，可以通过状态机状态直接观察。
- 空拍来源可以通过 FIFO empty、payload valid、状态跳转等信号定位。
- 后续调整发送节奏时，不需要重新梳理一长串硬延迟 tap。

**关键机制/信号：**

- 弹性 FIFO：缓存上游 payload 数据。
- 发送状态机：控制 payload、idle、refresh 或控制阶段切换。
- FIFO empty/full 或 almost full：判断供数和反压情况。
- payload valid / read enable：观察真实 payload 消费。
- 发送状态 debug 信号：用于 ILA 或仿真定位链路阶段。

**解决的问题：**

该重构主要解决两个问题。第一，减少硬延迟线带来的时序耦合和调试困难；第二，减少无效空拍对 payload 连续性的影响，让发送侧更容易保持稳定数据流。

**面试展开说法：**

可以这样讲：

> 我没有把 8B/10B 编码本身当作亮点，重点是发送链路结构重构。原来硬延迟线结构不容易看出数据卡在哪里，我改成 FIFO + FSM 后，数据缓存、发送调度和控制阶段分开了。ILA 里看 FIFO 水位、读使能和状态机状态，就能判断空拍来自上游供数、状态切换还是链路控制阶段。

---

## 3.7 发送侧 Normal / Idle / Refresh 调度

**简历表述：**  
设计发送侧数据调度逻辑，将 DDR 读出的 view 数据组织为 GTY 双 lane 发送数据流，支持正常数据、空闲数据和刷新控制数据的统一调度。

**实现方式：**

发送侧调度分为 normal、idle、refresh 三条业务路径。这里的 idle 是业务层 idle frame，不是 8B/10B 物理层 idle symbol。

normal 路径用于发送真实 view 数据。该路径依赖 DDR 读出的 payload 数据，当 DDR 读 FIFO 和发送侧 FIFO 有有效数据，且链路处于可发送状态时，状态机推进 normal 数据发送。

idle 路径用于没有正常 view 数据时维持上层发送节奏。idle 数据不从 DDR FIFO 读取，而是由 idle 数据生成逻辑产生固定结构。这可以避免发送侧在无 view 数据时进入不可控状态，也方便接收端看到可解析的业务空闲帧。

refresh 路径用于链路状态刷新或控制同步，优先级高于 normal 和 idle。触发 refresh 时，发送状态机会优先输出 refresh 控制数据，完成后再回到普通发送流程。

发送推进受实际 handshake 或 channel 状态约束。只有当前 beat 真正被发送/接受时，状态机计数、FIFO 读使能和 CRC 相关逻辑才推进。这一点保证了反压或链路未 ready 时不会误读 FIFO 或错推进帧边界。

**关键机制/信号：**

- normal 数据路径：DDR 读出 view 数据。
- idle 数据路径：`idle_frame_gen` 或等价 idle 数据生成逻辑。
- refresh 数据路径：高优先级刷新控制数据。
- `idle_process_en`：idle 过程指示。
- `refresh_process_en`：refresh 过程指示。
- `view_tx_done`：发送侧 view 完成反馈。
- 发送状态机当前状态：判断当前处于 normal、idle 还是 refresh。

**解决的问题：**

发送链路不能只考虑有数据时的 normal 路径。实际系统会遇到 DDR 暂时无 view、通信状态刷新、链路需要维持节奏等场景。normal/idle/refresh 统一调度让发送端在不同业务状态下都有明确行为，减少状态机停滞或输出无定义数据的风险。

**面试展开说法：**

可以这样讲：

> 我这里的 idle 不是 8B/10B 物理 idle 字符，而是业务层 idle frame。normal 发送真实 view，idle 在没有 view 时维持业务发送节奏，refresh 用于状态刷新。三条路径统一由发送状态机调度，并且只在实际发送 beat 被接受时推进，这样能保证反压情况下 FIFO、CRC 和帧边界不会错位。

---

## 3.8 CRC、Tag 与 Watchdog 诊断

**简历表述：**  
实现发送侧 CRC、frame tag 检查、first-view 标记和 no-EOF watchdog 诊断逻辑，便于通过状态机、FIFO 水位和发送计数定位链路空拍、帧边界和数据异常问题。

**实现方式：**

发送侧诊断逻辑围绕“数据是否正确、边界是否正确、状态机是否正常推进”三个方向设计。

CRC 逻辑在 frame 起点清零，在 payload 阶段累加，在 frame 尾部进行捕获或检查。为了避免反压导致 CRC 错推进，CRC 累加和清零都与实际发送 beat 绑定，只在有效发送时推进。

frame tag 检查用于确认 header/data 的关键标识字段是否符合预期。如果 tag 错误，通常说明数据错位、帧边界错误或上游 payload 选择错误。first-view 标记用于识别一轮发送中的首个 view，系统联调时可以用它对齐业务边界。

no-EOF watchdog 用于监控发送状态是否长时间没有走到 frame 结束。如果状态机卡在某个阶段、计数条件错误、FIFO 读空后没有恢复，watchdog 可以给出诊断脉冲，提示发送过程没有正常闭合。

调试时，这些诊断信号通常要结合状态机、FIFO 水位和发送计数一起看。例如：

- FIFO 水位正常但 tag 错，可能是帧边界或数据选择问题。
- FIFO 读空且 no-EOF 触发，可能是供数不足或读请求调度问题。
- CRC 错但 tag 正常，可能是 payload 数据路径或 CRC 对齐问题。

**关键机制/信号：**

- CRC clear / CRC enable：控制 CRC 清零和累加。
- frame tag 检查信号：判断 header/data 标识是否正确。
- `first-view` 标记：用于业务边界对齐。
- no-EOF watchdog：监控长时间没有 frame 结束。
- 状态机 debug 信号：定位当前发送阶段。
- FIFO 水位和读使能：判断供数是否正常。

**解决的问题：**

高速链路出错时，只知道“数据错了”并不够。CRC、tag、first-view 和 watchdog 可以把问题分层定位到缓存供数、数据错位、帧边界、CRC 对齐或状态机推进异常，缩短 ILA 抓波后的排查路径。

**面试展开说法：**

可以这样讲：

> 诊断逻辑不是只做一个 CRC error。CRC 用来看数据完整性，tag 用来看帧边界和数据类型是否错位，first-view 用来对齐业务起点，no-EOF watchdog 用来发现状态机长时间没有闭合。调试时我会把这些信号和 FIFO 水位、状态机状态一起看，快速判断问题发生在供数、组帧还是发送推进。

---

## 4. 可补充的面试问答

### 4.1 View 级管理和普通连续读 DDR 有什么区别？

普通连续读 DDR 只关心地址递增和读返回有效；view 级管理还关心业务边界。系统通过 `view_size` 判断一个 view 的读出边界，通过 `wr_view_num` 和 `rd_view_num` 判断是否有完整 view 可读，通过 `rd_latest_addr` 保存 view 起点推进关系。这样可以在发送完成、最后 view 判断和 replay/backtracking 时都有明确边界。

### 4.2 为什么 replay 选择 view 粒度？

CT 数据本身按 view 组织，接收端也更容易以 view 为边界重新对齐。如果从任意 beat 回退，可能导致接收端拿到半个 view 或业务帧错位。按 view 起点回退虽然粒度更粗，但恢复边界更清晰，系统复杂度更低。

### 4.3 为什么要区分业务 idle frame 和 8B/10B idle symbol？

业务 idle frame 是上层协议中的空闲业务帧，会经过发送状态机、CRC/诊断和后级链路封装。8B/10B idle symbol 是物理/链路层在没有 payload 时填充的控制字符，不属于业务 payload。二者层级不同，不能混在一起描述。

### 4.4 为什么 DDR fast mock 不强调精确时序？

因为该验证目标是缓存调度策略，而不是 DDR 物理时序精确性。fast mock 只需要制造足够代表性的压力，例如写持续输入、读返回延迟、读侧反压和回退读，就能快速暴露 overflow、underflow、状态机卡死和地址回退错误。

### 4.5 为什么弱化 Aurora 64B/66B IP？

因为该部分主要是成熟 IP 调用和接口适配，简历价值不如自研 DDR 缓存调度、view replay 和 GTY 8B/10B 发送链路重构。可以在面试中说明项目存在 64B/66B 跨板接收链路，但核心贡献应放在自研逻辑上。

